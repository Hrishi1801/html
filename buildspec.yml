version.0.2

phases:
  install:
    runtime-versions:
      java: corretto11
  pre_build:
    commands:
      - echo nothing to do in the pre_build phase...
  build:
    commands:
      - echo Build sytarted on "date"
  post_build:
    commands:
      - echo Build completed on "date"
  artifacts:
    files:
      -'**/*"
  
    
